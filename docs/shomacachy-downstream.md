# shomacachy downstream

`shomacachy` is intentionally lightweight and should not become a second full setup layer.

Examples of allowed downstream deltas:

- add/remove personal apps
- waybar preference changes
- personal config overrides

It should reuse `omacachy` install/update flow rather than reimplement system compatibility setup.
